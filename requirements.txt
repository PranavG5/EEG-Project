# Core signal processing and ML
mne>=1.6.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pywavelets>=1.4.0
matplotlib>=3.8.0
pandas>=2.1.0
joblib>=1.3.0

# Deep learning (EEGNet). Optional — the classical pipeline runs without it.
torch>=2.1.0

# Hardware layer. BrainFlow is the vendor-agnostic driver for OpenBCI,
# Neurosity, g.tec Unicorn, Muse and ~40 other boards; it is what lets the
# choice of headband be a command-line flag instead of a rewrite.
brainflow>=5.10.0

# Notebooks
jupyter>=1.0.0
